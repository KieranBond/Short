version: '3.5'

services:
  redis:
    image: redis:latest
    command: [ "redis-server", "/redis.conf" ]
    ports:
      - "6379:6379"
    volumes:
      - ./config/redis.conf:/redis.conf
      - ./.redis:/data